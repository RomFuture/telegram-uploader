tail -f telegram-uploader.log --- ЛОГИ

Рефакторинг use_case - мысли
- Почему test_client_api.py лежит в use_case? Это же прям вроде эталонный пример модуля в infrastructure. ---- данный модуль нужен, чтобы вынести сценарий теста от адаптера (в use_cases) -отвязка его от провайдера, который мы используем (если бы это был бы Макс, то нам бы пришлось писать под копирку test) - после того, как мы обработали сценарий (нашли, какое сообщение посылать) мы отправляет год ошибки напрямую через storage_provider.
- Что такое DTO? Для чего они нужны? И как используются в приложении? Расскажи, как обычно имплементируют DTO в bigtech
- backup_api.py. Для чего нам Гуи сценарий в Use_cases? Объясни подробнее
- Зачем нам worker сценарии в use_cases слое? Для чего мы задаём тут сценарии для воркеров? 
- Почему у нас есть членение на BackupApi и WorkerApi? Для чего оно нужно и что означает? 
- Как устроена create_database.py? Как происходит создание новой датабазы в таблице?
- Почему мы напрямую вызываем ошибки из Domain, хотя у нас есть API и нам не нужно лезть "в кухню", чтобы получить конкретную ошибку ( к примеру в файле unlock_session.py)
- Как устроена session.py. Как мы можем отдавать команды не зная кому и это будет работать? Такой метод вообще применяется в bigtech?
- Как думаешь мой проект соответсвует стандартам bigtech? Если нет, то в чём?
- Почему файлы list_session_profiles.py, list%folders.py содержат буквально по 13 строк? Смотри, я вот что заметил - думаю, что мы можем объеденить все файлы list_* в один файл ( create_* - так же можем и с ним поступить)
- session нам нужна только для связи с датабазой?

ОСТАНОВИЛСЯ на backup/


1 - Сделай нормальное название у @src/use_cases/telegram/test_client_api.py 
2 - Файл сессии на диске, заполненность полей Settings --- Что это значит? На каком диске находится сессия? Я думал она есть только в датабазе
3 - UC не знает про Tkinter и пути из ~/.config/... — только «дай мне готовый StorageProviderPort и chat_id». --- понимаю, но не понимаю - как это происходит?
4 - Client API migration (bot → client) не ломала диагностику — меняется только provider impl ---- не понимаю, что такое диагностика и чем она тут мешает? 
5 - backup/restore/test делили один контракт StorageProviderPort; --- что такое контракт? 

(Идеи на реализацию → [BACKLOG.md](../BACKLOG.md), не сюда.)
